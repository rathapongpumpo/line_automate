"use client";import{useState}from"react";import{useRouter}from"next/navigation";import{Eye,EyeOff,MessageCircleMore}from"lucide-react";import{Button}from"@/components/ui";
export default function LoginPage(){const demo=process.env.NODE_ENV!=="production";const router=useRouter();const[email,setEmail]=useState(demo?"admin@demo.local":"");const[password,setPassword]=useState(demo?"demo1234":"");const[show,setShow]=useState(false);const[error,setError]=useState("");const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setError("");setBusy(true);try{const response=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});const body=await response.json()as{error?:string};if(!response.ok){setError(body.error||"เข้าสู่ระบบไม่สำเร็จ");return}router.replace("/dashboard");router.refresh()}finally{setBusy(false)}}return <main className="surface-grid grid min-h-dvh place-items-center p-4"><section className="w-full max-w-[440px] rounded-2xl border border-slate-200 bg-white p-6 shadow-[0_20px_60px_rgba(15,23,42,.12)] sm:p-8"><div className="flex items-center gap-3"><div className="grid size-11 place-items-center rounded-xl bg-[#06c755] text-white"><MessageCircleMore/></div><div><h1 className="text-xl font-bold">LINE Sales Assistant</h1><p className="text-sm text-slate-500">เข้าสู่ระบบจัดการลูกค้า WLB Store</p></div></div><form noValidate onSubmit={submit} className="mt-8 space-y-5"><label className="block"><span className="mb-2 block font-medium">อีเมล</span><input className="control w-full px-3" type="email" autoComplete="username" value={email} onChange={e=>setEmail(e.target.value)} aria-invalid={Boolean(error)}/></label><label className="block"><span className="mb-2 block font-medium">รหัสผ่าน</span><span className="relative block"><input className="control w-full px-3 pr-12" type={show?"text":"password"} autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} aria-invalid={Boolean(error)} aria-describedby="login-error"/><button type="button" onClick={()=>setShow(v=>!v)} aria-label={show?"ซ่อนรหัสผ่าน":"แสดงรหัสผ่าน"} className="absolute right-1 top-0 grid size-11 place-items-center text-slate-500">{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></span></label><div id="login-error" className="min-h-5 text-sm text-red-600" role="alert">{error}</div><Button type="submit" busy={busy} className="w-full">เข้าสู่ระบบ</Button></form>{demo&&<div className="mt-6 rounded-xl bg-slate-50 p-4 text-sm"><div className="font-semibold">บัญชีสำหรับ Demo</div><div className="mt-2 grid grid-cols-[90px_1fr] gap-y-1 text-slate-600"><span>อีเมล</span><code>admin@demo.local</code><span>รหัสผ่าน</span><code>demo1234</code></div></div>}</section></main>}
