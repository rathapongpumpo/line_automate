import{cookies}from"next/headers";import{redirect}from"next/navigation";import{verifySession}from"@/lib/auth";export default async function Home(){const session=(await cookies()).get("lsa_session")?.value;redirect(verifySession(session)?"/dashboard":"/login")}
